class Rectangle {
    _width;
    _height;
    _x;
    _y;
    constructor(x, y, width, height) {
        this._height = height;
        this._width = width;
        this._x = x;
        this._y = y;
    }

    /**
     *
     * Pos = center report
     */
    getPos = () => {
        return   
    }
}